import { ValidateActionButtonDirective } from './validate-action-button.directive';

describe('ValidateActionButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateActionButtonDirective();
    expect(directive).toBeTruthy();
  });
});
