import {environment} from "../../environments/environment";


export const COMMENTS = environment.HOST_BACKEND + "comments"
export const TEAMS = environment.HOST_BACKEND + "teams"
export const LOGIN = environment.HOST_BACKEND + "auth/login"
export const REGISTER = environment.HOST_BACKEND + "users"
export const PROJECT = environment.HOST_BACKEND + "projects"
export const TASK = environment.HOST_BACKEND + "tasks"
